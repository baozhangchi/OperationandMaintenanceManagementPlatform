@page "/drivers"
<PageTitle>Drivers</PageTitle>
<DriversLogView @ref="LogView"></DriversLogView>

@code {

    protected LogViewBase? LogView { get; set; }

    // protected override Task OnInitializedAsync()
    // {
	   //  if (LogView != null)
    //     {
	   //      return LogView.ReloadData();
    //     }

	   //  return Task.CompletedTask;
    // }
}